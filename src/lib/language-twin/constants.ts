// Below this many real language_evidence rows, the profile is too thin to
// show honestly — Overview, Progress, and Today all gate on the same
// number so "not enough data" means the same thing everywhere.
export const MIN_EVIDENCE_FOR_PROFILE = 15;

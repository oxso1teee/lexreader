-- P0-АУДИТ 3.11: new_cards_per_day ограничивал очередь только В РАМКАХ
-- одного открытия страницы повторения — ничего не отслеживало, сколько
-- новых карточек уже показано сегодня, так что повторное открытие снова
-- выдавало полную порцию новых карточек. first_reviewed_at фиксируется один
-- раз, при первом же прохождении карточки (repetitions 0 -> 1+), и
-- используется, чтобы вычесть уже выданные сегодня новые карточки из лимита.
alter table srs_state add column first_reviewed_at timestamptz;

-- Раздел 5 промта 2026-07-30 (полировка): один сквозной уровень вместо
-- трёх разных счётчиков (слова/карточки/стрик) — растёт от всех действий
-- сразу, звание считается на лету по порогам в src/lib/ranks.ts.
alter table profiles add column xp int not null default 0;

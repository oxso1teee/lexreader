# 02 — Баги, открытые прямо сейчас (проверено 22.08.2026)

Формат каждого пункта: где, что не так, как воспроизвести, как чинить.
Всё ниже — либо найдено заново сегодня (раздел A), либо перепроверено из
старых аудитов и подтверждено ещё открытым (раздел B), либо не
перепроверено построчно и требует подтверждения перед тем как чинить
(раздел C — не трогай их вслепую, сначала проверь, актуальны ли).

## A. Найдено заново сегодня

### A.1 Уязвимость в pdfjs-dist — произвольное исполнение JS при открытии PDF (ВЫСОКИЙ)

`npm audit` показывает: `pdfjs-dist >=5.6.83 <6.2.108`, установлена
версия из этого диапазона, содержит
[GHSA-hq66-cqwq-w95j](https://github.com/advisories/GHSA-hq66-cqwq-w95j) —
произвольное исполнение JavaScript при открытии специально собранного
PDF. У тебя есть фича импорта PDF (`src/app/(app)/library/new/pdf-import-form.tsx`
судя по списку файлов) — пользователь загружает чужой/сторонний PDF,
библиотека его парсит на клиенте через `pdfjs-dist`. Это прямой путь
эксплуатации: подсунуть вредоносный PDF под видом "учебного текста".

**Фикс**: `npm audit fix` (без `--force` — фикс доступен в пределах
текущего диапазона зависимостей, `next` трогать не должно). Сделать в
первую очередь, это самое дешёвое и самое опасное из новых находок.

### A.2 4 high-severity CVE в самом Next.js (ВЫСОКИЙ, но дороже чинить)

`npm audit`: Next.js `16.2.10` содержит 4 high-severity уязвимости —
SSRF через rewrites с контролируемым атакующим hostname, DoS через SVG в
Image Optimization API, раскрытие внутренних Server Function endpoints
без аутентификации, неограниченный payload Server Action в Edge runtime.
Фикс требует `next@16.3.2` — **вне** текущего диапазона `package.json`
(`"next": "16.2.10"` без `^`), то есть `npm audit fix --force` его
поставит, но это осознанное решение об апгрейде мажорной/минорной
версии фреймворка, не автоматический безопасный патч.

**Что делать**: не патчить вслепую одной командой. Завести отдельную
задачу "обновить Next.js до 16.3.2", прогнать после этого полный набор
тестов + e2e + вручную пройти самые чувствительные потоки (оплата,
загрузка файлов, server actions), и только потом мержить. Не блокирует
остальной план, но должно быть сделано до релиза в сторы — сторы (особенно
Apple) чувствительны к известным уязвимостям в SDK/зависимостях при
ревью, хотя формально не сканируют npm-зависимости веб-бэкенда.

### A.3 Нет рабочего контактного email в Условиях/Политике (ВЫСОКИЙ для реального релиза)

Проверил `src/app/terms/page.tsx` и `src/app/privacy/page.tsx` —
упоминания email/контакта для связи по вопросам приватности/условий не
нашёл вообще (в июльском аудите там был `support@lexreader.example` —
несуществующий зарезервированный домен; сейчас похоже, что его вообще
убрали, а не заменили на настоящий). И App Store, и Google Play **на
уровне процесса ревью и юридически обязательных полей формы** требуют
рабочий контакт для запросов на удаление данных/поддержку. Без этого
карточку приложения не примут технически (поле в форме обязательное), а
если вписать туда что попало — это лживое поле, что тоже основание для
отклонения.

**Фикс**: завести реальный email (даже простой, на своём домене/Яндексе)
именно под поддержку LexReader, вписать в Terms, Privacy, и в форму
Data Safety/Privacy Nutrition Label (файл 05).

## B. Перепроверено из старых аудитов — подтверждено, ещё открыто

### B.1 Кэш переводов может быть отравлен любым пользователем (СРЕДНИЙ-ВЫСОКИЙ)

Из `docs/PRELAUNCH_AUDIT_2026-07-23.md`, раздел 3.1 — не нашёл следов
исправления в текущих миграциях (`grep` по `translations_cache` не
показывает новой политики, ограничивающей `insert`). Проверь
`supabase/migrations/*translat*` и `src/app/api/translate/route.ts`
своими глазами перед тем, как чинить — я не проверял это построчно
сегодня, только что не увидел явной миграции-фикса в списке файлов.

**Сценарий**: пользователь напрямую вставляет заведомо неверный/
оскорбительный перевод для частого слова раньше настоящего запроса —
из-за `unique(source_text, source_lang, target_lang)` и
`ignoreDuplicates: true` эта запись побеждает навсегда для всех
пользователей, включая платных.

**Фикс**: то же, что предлагалось в июле — либо `insert` только через
`service_role` (сервер сам пишет в кэш после успешного вызова MyMemory,
клиент никогда не пишет напрямую), либо триггер на `before insert`,
валидирующий, что запись создаётся из серверного контекста.

### B.2 Гонка при проверке лимита переводов 30/мин (СРЕДНИЙ)

`docs/PRELAUNCH_AUDIT_2026-07-23.md`, раздел 3.3 — `select count` потом
`insert`, не атомарно. Не нашёл следов перехода на атомарную проверку
(например, RPC-функцию с `SELECT ... FOR UPDATE` или Postgres advisory
lock). Не блокирует релиз в сторы, но стоит закрыть параллельно с
разделом 3 (Безопасность), пока не набралась реальная пользовательская
база, на которой это дорого воспроизводить и чинить.

## C. Требует перепроверки перед тем, как чинить (не трогать вслепую)

Эти пункты — из `docs/SECOND_PASS_AUDIT_FINDINGS.md` (23.07), я **не**
перепроверял их построчно сегодня (в отличие от раздела 3 файла 01, где
проверил явно). Не факт, что ещё актуальны — возможно, закрыты в рамках
масштабного рефакторинга Мозга/Тетради за последний месяц (миграции FSRS,
unified UI slices 1-11). Перед тем как чинить — сначала подтверди грепом
по актуальному коду.

- **Переключение вкладки "Чтение"↔"Повтор" в Тетради сбрасывает
  прогресс текущей сессии практики** (`notebook-client.tsx`). Я
  попытался найти этот паттерн в текущем файле сегодня — не нашёл
  прежней структуры условного рендера, похоже компонент переписан в
  рамках unified UI slices. Скорее всего уже неактуально, но подтверди.
- **Онбординг: Шаг 1 (целевой язык) не исключает язык, ещё не выбранный
  на Шаге 2 как родной, реактивно** — вероятно, минорно, не проверял.
- **Настройка "Направление изучения" (Study Direction) не влияет
  реально ни на один режим повторения** — из июльского аудита; учитывая,
  что весь SRS переехал на FSRS с тех пор, эта настройка могла быть
  либо реализована, либо вовсе убрана в рамках рефакторинга. Проверь
  `brain/settings/settings-form.tsx` и режимы повторения.
- **Отдельные `has*Room`-проверки лимитов бесплатного тарифа обходятся
  напрямую через PostgREST** — миграция `0019_free_tier_db_backstop.sql`
  существует и, судя по названию, закрывает именно это на уровне БД.
  Вероятно уже исправлено — но не проверял, какие именно таблицы она
  покрывает (тексты/колоды/карточки/словарь — все четыре или часть).

## Как использовать этот файл

Не бросайся чинить всё сразу. Порядок: A.1 (10 минут, `npm audit fix`) →
A.3 (15 минут, завести email) → A.2 (отдельная задача, с тестированием)
→ B.1 → B.2 → перепроверить раздел C и завести отдельные issue на то,
что подтвердится. Раздел C — не делай изменений, пока не убедился
грепом/чтением кода, что баг ещё жив; иначе рискуешь чинить то, что уже
починено, и терять на этом время.

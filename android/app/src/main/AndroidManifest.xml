<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- src/components/mic-button.tsx — голосовой ввод слова через Web
         Speech API внутри WebView. Capacitor's BridgeWebChromeClient сам
         прокидывает системный runtime-permission диалог на web-запрос
         getUserMedia/SpeechRecognition, но без этой декларации в манифесте
         запрос отклоняется ещё до диалога. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- src/app/(app)/library/new/photo-import-form.tsx (capture="environment")
         и два обычных accept="image/*" file input'а (import-modal.tsx OCR-
         импорт, notebook/word-row.tsx фото слова) — та же логика, что и
         RECORD_AUDIO выше: Capacitor's BridgeWebChromeClient сам прокидывает
         системный runtime-permission диалог на web-запрос камеры, но без
         этой декларации в манифесте запрос отклоняется ещё до диалога. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 13+ (API 33) требует этого разрешения для ЛЮБОГО показа
         уведомлений, включая те, что service worker (public/sw.js)
         показывает через self.registration.showNotification() на push-
         событие — существующий Web Push (VAPID) остаётся как есть в этой
         версии обёртки (нативный FCM/APNs — файл 04 раздел 3, отдельный
         патч), это разрешение нужно, чтобы то, что уже работает, не
         тихо перестало работать конкретно на Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
